version: '3.1'

services:
  api:
    image: {{BACKEND_DOCKER_IMAGE}}
    restart: always
    networks:
      - apinet
    ports:
      - 9998:9998
    environment:
      APP_PORT: 9998
      SWAGGER_HOST: 172.31.95.147

networks:
  apinet:
    driver: bridge