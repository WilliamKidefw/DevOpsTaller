version: '3.1'

services:

  api:
    image: {{BACKEND_DOCKER_IMAGE}}
    restart: always
    networks:
      - apinet
    ports:
      - 9998:5000
    environment:
      APP_PORT: 9998
      SWAGGER_HOST: 184.72.110.98

networks:
  apinet:
    driver: bridge